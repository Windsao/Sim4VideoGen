diff --git a/examples/wanvideo/model_training/train.py b/examples/wanvideo/model_training/train.py
index 37494e7..213f92a 100644
--- a/examples/wanvideo/model_training/train.py
+++ b/examples/wanvideo/model_training/train.py
@@ -22,7 +22,9 @@ class WanTrainingModule(DiffusionTrainingModule):
         super().__init__()
         # Load models
         model_configs = self.parse_model_configs(model_paths, model_id_with_origin_paths, enable_fp8_training=False)
-        self.pipe = WanVideoPipeline.from_pretrained(torch_dtype=torch.bfloat16, device="cpu", model_configs=model_configs)
+        # Use local tokenizer config to avoid downloading from ModelScope
+        local_tokenizer_config = ModelConfig(path="/nyx-storage1/hanliu/world_model_ckpt/Wan-AI/Wan2.1-T2V-1.3B/google/umt5-xxl")
+        self.pipe = WanVideoPipeline.from_pretrained(torch_dtype=torch.bfloat16, device="cpu", model_configs=model_configs, tokenizer_config=local_tokenizer_config)
         
         # Training mode
         self.switch_pipe_to_training_mode(
